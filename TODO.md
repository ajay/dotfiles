# TODO
- [ ] Add `npm` dep (probably in `build-tools`)
- [ ] Switch terminal to alacritty or equivalent (defaults, shortcuts, workspace-setup)
- [ ] Switch dotbot-pip from submodule to local plugin; add functionality to specify pip binary and perform pip upgrades

# Done
