name: make-ci

on:
  push:
    branches: [master]
  schedule:
    - cron: '0 0 * * *'

jobs:
  make-ci:
    uses: ajay/build-tools/.github/workflows/make-ci.yml@main
    with:
      submodule-checkout-opt-out: private
